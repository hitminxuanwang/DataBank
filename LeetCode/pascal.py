class Solution:
	def generate(self,numRows):
		result=[]
		result.append([1])
		result.append([1,1])
		for i in range(0,numRows):
		   	temp[]
		#	for j in range(0,i):
		#		temp[j]=result[i][j]
		return result
solution=Solution();
data=solution.generate(5)
print(data)
